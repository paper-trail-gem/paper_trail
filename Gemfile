# frozen_string_literal: true

source "https://rubygems.org"
gemspec

# For now, we get the associations gem from git, which can't be done in the
# gemspec; must be done in a gemfile (git is a bundler feature, not a rubygems
# feature)
gem(
  "paper_trail-association_tracking",
  github: "westonganger/paper_trail-association_tracking"
)
